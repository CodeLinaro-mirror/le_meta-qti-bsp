#@TYPE: Machine
#@NAME: alor
#@DESCRIPTION: Machine configuration for QTI HDK with alor
BASEMACHINE ?= "alor"
require conf/machine/include/basemachine.inc

DEFAULTTUNE ?= "aarch64"
include conf/machine/include/arm/arch-armv8a.inc

require recipes-kernel/linux-msm/msm-common.inc

PREFERRED_VERSION_synx-kernel ?= "1.1"
PREFERRED_VERSION_securemsmdlkm ?= "1.1"
PREFERRED_VERSION_mod-blacklist ?= "2.0"

# clang version for kernel toolchain 6.12
CLANG_VERSION = "r536225"

#### KERNEL ####
KERNEL_SRC_TYPE = "soc-repo"
MACHINE_USES_KERNEL_PREBUILTS = "True"
KERNEL_VARIANT:qti-distro-debug ?= "debug-defconfig"
KERNEL_VARIANT:qti-distro-perf ?= "defconfig"
KERNEL_MODULES_LIST = "modules-lists/modules.list.msm.alor-le"
KERNEL_MODULES_BLACKLIST = "\
    modules.vendor_blocklist.msm.alor-le \
    modules.systemdlkm_blocklist.msm.alor-le \
"
KERNEL_DEFCONFIG_VARIANT:qti-distro-debug ?= "debug-defconfig"
KERNEL_DEFCONFIG_VARIANT:qti-distro-perf ?= "defconfig"

# Check for msm-kernel or gki_kernel
KERNEL_TYPE = "dist"

#extra module blacklist
EXTRA_BLACKLIST_MOD ?= ""

# enable DDK build
DDK_BUILD = "true"
TARGET_BOARD_PLATFORM = "alor-le"
HOSTTOOLS += "${@bb.utils.contains('DDK_BUILD', 'true', 'rsync', '', d)}"

KERNEL_CONFIG  ?= "build.config.msm.alor.le"
KERNEL_BUILD_CONFIG = "soc-repo/${KERNEL_CONFIG}"

#### dtb ####
KERNEL_DTB_NAMES = "alor.dtb alorp.dtb alorp-iot.dtb"

MACHINE_SUPPORTS_DTBO= "True"
BUILD_WITH_TECHPACKS = "1"
SKIP_VENDOR_BOOT = "False"

#### boot.img ####
KERNEL_IMAGETYPE = "Image"
KERNEL_BASE = "0x80000000"
KERNEL_TAGS_OFFSET = "0x80000000"
BOOT_HEADER_VERSION = "2"

#### systemd ####
SERIAL_CONSOLE ?= "115200;ttyMSM0"

#### cmdline ####
CONSOLE_PARAM:qti-distro-perf  = ""
CONSOLE_PARAM:qti-distro-debug = "console=ttyMSM0,115200n8 kasan.stacktrace=off stack_depot_disable=off page_owner=on no_hash_pointers panic_on_taint=0x20 slub_debug=FZP,zs_handle,zspage;FZPU qcom_geni_serial.con_enabled=1 earlycon ufshcd_core.uic_cmd_timeout=2000"
HIBERNATION_PAPAMS = "hibernate=nocompress"
KERNEL_CMD_PARAMS = "${CONSOLE_PARAM}"

#### others ####
PAGE_SIZE ?= '4096'
SDCARD_DEVICE = "mmcblk1p1"

#Abl would be enabled later
EXTRA_IMAGEDEPENDS += "edk2"
PREFERRED_VERSION_edk2 ?= "4.0"

# TODO: Temporary change to override BB_SIGNATURE_HANDLER set in local.conf.
# Other machines are already supporting OEEquivHash as hashing signature.
# Fix build issues observed in this machine and remove this override.
BB_SIGNATURE_HANDLER = "OEBasicHash"

#QSEEComCompat and QSEECom are mutually exlusive and QSEEComCompat depends on  smcinvoke
#lv-build builds the required listernes , le_qseecom enables qseecomd
MACHINE_FEATURES += "dm-verity-none"

MACHINE_MNT_POINTS = " \
	/persist \
	/firmware \
	/bt_firmware \
	/dsp \
	/overlay \
"

SYSTEM_SIZE_EXT4   ?= "4026531840"
PERSIST_SIZE_EXT4 ?= "7MiB"
# Define userdata dir, size to be used
USERDATA_DIR = "${@bb.utils.contains('MACHINE_MNT_POINTS', '/overlay', 'overlay', 'data', d)}"
USERDATA_SIZE ?= "60GiB"

# Formats of root filesystem images
IMAGE_FSTYPES += "ext4"
MACHINE_SUPPORTS_PDMAPPER = "True"
MACHINE_SUPPORTS_ANDROID_PROPERTIES = "True"
MACHINE_FSCONFIG_CONF = "alor-fsconfig.conf"
MACHINE_SUPPORTS_DISPLAY = "False"
MACHINE_SUPPORTS_SSR = "True"

USBCOMPOSITION = "901D"

# Partition conf
MACHINE_PARTITION_CONF = "alor-partition.conf"

# File indicating firmware residing places on device.
MACHINE_FILESMAP_CONF = "alor-filesmap"

### MACHINE_FEATURES ###
#MACHINE_FEATURES += "qti-ab-boot qti-adsp qti-cdsp fastrpc qti-tzlog qti-crypto qti-security qseecompat qti-smciqseecomd"
MACHINE_FEATURES += "qti-uvc qti-ab-boot qti-adsp qti-cdsp fastrpc qti-tzlog qti-crypto qti-security qti-audio qti-video qti-rpmb qseecompat qti-smciqseecomd smcinvoke file-based-encryption qti-tmecom"

#Temp change for bringup dry run
DISTRO_FEATURES:remove += "qti-sensors qti-sensors-prop qti-location"

# On msm-5.4 or above ufs driver supports BSG Device.
UFS_BSG_DEV_USAGE = "True"

DISTRO_FEATURES:remove = "qti-fastcv ros2 qirp-sdk qti-ib2c qti-auto-framing-stabilization"
DISTRO_FEATURES:append = " qti-uvc"

QPERMISSIONS_ENABLE = "1"

# SELinux is set in enforcing mode, by default, as below;
# DEFAULT_ENFORCING ?= "enforcing"
# During bring up, to unblock the technology teams, SELinux is set to permissive.
##############################################################
DEFAULT_ENFORCING = "enforcing"

# pack for installation of ESDK
COREBASE_FILES:append = " meta-poky"
