From 34589a0d620bfacba922e97dd87553bf8bc8627d Mon Sep 17 00:00:00 2001
From: Mrunali Nesarkar <mnesarka@qti.qualcomm.com>
Date: Tue, 27 May 2025 12:39:24 +0530
Subject: [PATCH] Subject: [PATCH] systemd: Disable unused mount points

---
 src/basic/random-util.c  |  7 ++++++-
 src/shared/mount-setup.c | 12 ++++++------
 2 files changed, 12 insertions(+), 7 deletions(-)

diff --git a/src/basic/random-util.c b/src/basic/random-util.c
index 227b82f790..218a431d89 100644
--- a/src/basic/random-util.c
+++ b/src/basic/random-util.c
@@ -242,7 +242,12 @@ int genuine_random_bytes(void *p, size_t n, RandomFlags flags) {
 
         fd = open("/dev/urandom", O_RDONLY|O_CLOEXEC|O_NOCTTY);
         if (fd < 0)
-                return errno == ENOENT ? -ENOSYS : -errno;
+		if (fd < 0){
+	    		system("mknod -m 444 /dev/urandom c 1 9");
+	    		fd = open("/dev/urandom", O_RDONLY|O_CLOEXEC|O_NOCTTY);
+		       	if (fd < 0)
+			  	return errno == ENOENT ? -ENOSYS : -errno;
+		}
 
         return loop_read_exact(fd, p, n, true);
 }
diff --git a/src/shared/mount-setup.c b/src/shared/mount-setup.c
index 7ba579ef63..7bc2c49e3f 100644
--- a/src/shared/mount-setup.c
+++ b/src/shared/mount-setup.c
@@ -68,8 +68,8 @@ static const MountPoint mount_table[] = {
           NULL,          MNT_FATAL|MNT_IN_CONTAINER },
         { "devtmpfs",    "/dev",                      "devtmpfs",   "mode=755" TMPFS_LIMITS_DEV,               MS_NOSUID|MS_STRICTATIME,
           NULL,          MNT_FATAL|MNT_IN_CONTAINER },
-        { "securityfs",  "/sys/kernel/security",      "securityfs", NULL,                                      MS_NOSUID|MS_NOEXEC|MS_NODEV,
-          NULL,          MNT_NONE                   },
+//        { "securityfs",  "/sys/kernel/security",      "securityfs", NULL,                                      MS_NOSUID|MS_NOEXEC|MS_NODEV,
+//          NULL,          MNT_NONE                   },
 #if ENABLE_SMACK
         { "smackfs",     "/sys/fs/smackfs",           "smackfs",    "smackfsdef=*",                            MS_NOSUID|MS_NOEXEC|MS_NODEV,
           mac_smack_use, MNT_FATAL                  },
@@ -102,10 +102,10 @@ static const MountPoint mount_table[] = {
           cg_is_legacy_wanted, MNT_IN_CONTAINER     },
         { "cgroup",      "/sys/fs/cgroup/systemd",    "cgroup",     "none,name=systemd",                       MS_NOSUID|MS_NOEXEC|MS_NODEV,
           cg_is_legacy_wanted, MNT_FATAL|MNT_IN_CONTAINER },
-#if ENABLE_PSTORE
-        { "pstore",      "/sys/fs/pstore",            "pstore",     NULL,                                      MS_NOSUID|MS_NOEXEC|MS_NODEV,
-          NULL,          MNT_NONE                   },
-#endif
+//#if ENABLE_PSTORE
+//        { "pstore",      "/sys/fs/pstore",            "pstore",     NULL,                                      MS_NOSUID|MS_NOEXEC|MS_NODEV,
+//          NULL,          MNT_NONE                   },
+//#endif
 #if ENABLE_EFI
         { "efivarfs",    "/sys/firmware/efi/efivars", "efivarfs",   NULL,                                      MS_NOSUID|MS_NOEXEC|MS_NODEV,
           is_efi_boot,   MNT_NONE                   },
-- 
2.17.1

