require conf/distro/auto.conf

DISTRO_FEATURES:append = " qti-lxc qti-avb"
# Disable qti-lxc qti-avb on LeMans as features are not ready
DISTRO_FEATURES:remove:lemans = "qti-lxc qti-avb"

PRODUCT = "${MACHINE}lxc"
