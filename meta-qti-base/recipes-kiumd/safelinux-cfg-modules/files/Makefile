M=$(PWD)
SAFELINUX_MODULES_ROOT=$(PWD)
INC=-I/$(M)/include/*
KBUILD_OPTIONS+=SAFELINUX_MODULES_ROOT=$(SAFELINUX_MODULES_ROOT)
KBUILD_OPTIONS+=KERNEL_SRC=$(KERNEL_SRC)

all: modules

clean:
	$(MAKE) -C $(KERNEL_SRC) M=$(M) clean

%:
	$(MAKE) -C $(KERNEL_SRC) M=$(M) $(INC) $@ $(KBUILD_OPTIONS)
