From 3d0f9b119a2230bf9bd3a13eb2b57b02b4ccf643 Mon Sep 17 00:00:00 2001
From: Bharathraj Nagaraju <snbraj@codeaurora.org>
Date: Thu, 4 Aug 2016 11:19:02 -0700
Subject: [PATCH] Egldisplay should get destroyed before clearing the display

Change-Id: Ia2b156a0fa34e8b2ec443e6054b4e37149c4ddfa
Signed-off-by: Bharathraj Nagaraju <snbraj@codeaurora.org>
---
 ext/wayland/gstwaylandsink.c | 8 ++++++++
 1 file changed, 8 insertions(+)

diff --git a/ext/wayland/gstwaylandsink.c b/ext/wayland/gstwaylandsink.c
index ced28f8..b366638 100644
--- a/ext/wayland/gstwaylandsink.c
+++ b/ext/wayland/gstwaylandsink.c
@@ -453,6 +453,14 @@ gst_wayland_sink_change_state (GstElement * element, GstStateChange transition)
         if (sink->buffer_table)
           g_hash_table_remove_all(sink->buffer_table);
 
+        /* MSM8996: Egldisplay should get destroyed before clearing the display
+         * object.
+         */
+        if (sink->egldpy) {
+          eglTerminate(sink->egldpy);
+          sink->egldpy = EGL_NO_DISPLAY;
+        }
+
         g_clear_object (&sink->display);
         g_clear_object (&sink->pool);
       }
-- 
2.9.0.137.gcf4c2cf

